on: [push]

jobs:
  zlib:
    runs-on: windows-2019
    name: build zlib
    steps:
    - name: zlib-v140
      uses: williballenthin/siglib/action.yml@master
      with:
        VCPKG_PLATFORM_TOOLSET: 'v140'
        VCPKG_PACKAGE: 'zlib'
